# My personal site using Gatsby.

This is only my personal site, which I built using Gatsby, TypeScript and (quite)some plugins.

If you want to use it as a template, just download and modify accordingly.

To run, simply execute `npm run build`.